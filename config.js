// Bound — Supabase connection settings.
// Kept separate from index.html so your keys are never overwritten
// when the app itself is updated.

const SUPABASE_URL  = "https://sjwbvungziepsihpsrdo.supabase.co";

// PASTE YOUR KEY BELOW (replace the placeholder, keep the quotes).
//
// Where to find it:
//   Easiest -> the "Connect" button at the top of your project dashboard
//   Or      -> Settings (gear, bottom left) -> API Keys
//
// Two tabs are shown there. Either key works, but prefer the first:
//   1. "API Keys" tab        -> Publishable key, starts sb_publishable_...
//                               (recommended - legacy keys retire late 2026)
//   2. "Legacy API Keys" tab -> anon key, starts eyJ...
//
// If the API Keys tab shows a "Create new API keys" button, click it first.
// That's safe: it adds the new key alongside your existing one and
// nothing that currently works stops working.
//
// This key is meant to be public. Your data stays protected by the
// row-level security policies already set up on your tables.
const SUPABASE_ANON = "sb_publishable_rAiAyvfd-ho1tQwO-iIQ3Q_CHVk_wN8";
